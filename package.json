{
    "name": "chartjs-plugin-accessible-data",
    "version": "1.0.0",
    "source": "src/index.js",
    "main": "dist/main.cjs",
    "module": "dist/main.js",
    "scripts": {
        "clean": "rimraf dist .parcel-cache",
        "watch": "parcel watch",
        "build": "parcel build"
    },
    "devDependencies": {
        "parcel": "^2.12.0",
        "rimraf": "^5.0.5"
    }
}
